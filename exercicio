#include <stdio.h>
#include <stdlib.h>

int main()
{
    int v;
    int i, a, b;
   
    a=5000;
    b=0;
    
    for (i=0;i<a;i++)
    {
        v = malloc(xsizeof(int));
        v[i]=b;
        b++;
        printf("%d \n", v[i]);
    }
return 0;
}
